# DAO Package

This package contains Data Access Objects (DAOs) responsible for interacting with the database.
Key classes include:
- `BudgetLoader`: Handles loading data from the SQLite database into model objects.
- `BudgetModifier`: Handles updates and insertions into the database.
- `DatabaseManager`: Manages database connections and schema creation.
